# Implementation Tasks

- [ ] Replace `make dev` with `make docker-dev` to prevent blocking on log-follow
- [ ] Remove the `uv` install and `source` lines (not needed for Go project)
- [ ] Change `cd kodit` to `cd /home/retro/work/kodit` (absolute path)
- [ ] Commit and push the fixed startup script to the helix-specs branch
